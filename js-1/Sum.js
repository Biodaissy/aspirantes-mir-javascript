// escribe la función suma acá
function suma(number) {
  let resultado = 0;
  resultado = (number * (number + 1)) / 2;
  return resultado;
}

// código de prueba

console.log(suma(4)); // 1 + 2 + 3 + 4 = 10
console.log(suma(10)); // 55
console.log(suma(15)); // 120
